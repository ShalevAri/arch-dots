# This is the global `config.fish` file
# For organization purposes, the actual configurations are located in the `conf.d` directory, each in their own respective files.
# This `config.fish` file simply sources them all.
# The only thing I do in this file is load the theme, which is Gruvbox

theme_gruvbox dark medium
